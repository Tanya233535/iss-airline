server:
  port: 8080
  servlet:
    context-path: /iss

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/iss_airline_db
    username: postgres
    password: 111
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

  security:
    user:
      name: admin
      password: admin